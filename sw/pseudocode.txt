
rack array = [[0,0],[0,0]] [[A bot, A top], [B bot, B top]]
- track which racks have been visited (rack[m][n] += 1)

track which branch the robot is in (A/B)

6 boxes in each rack -> [0, 1, 2, 3, 4, 5]

unloading bay int (0,1,2,3) blue -> green -> yellow -> red


i/o
    - line sensor (left/right)
    - turn sensor (left/right)
    - reel sensor (left/right)
    - grabber servo (left/right, should be identical code for both)
    - wheel motor (left/right)
    - LED (R/G/B/Y)
    - push button

for loop A(code: RIGHT) / B (code: LEFT) (with respective direction)

    for line follow --> T junction --> RIGHT/LEFT
    for reel sensor --> RIGHT/LEFT


- reset logic
    - pushbutton for hard restart and reset memory 


- start box logic 
    - at start, go forward until line is found
    - once line is found, turn junction based on rack completion 
        - if (sum(rack[0]) == 12), turn right
        - else turn left
    - follow line based on line logic

- line detection (narrow trapezium of sensors)
    - if both front are ON, continue until either/both rear is ON
    - if one front is OFF, correct in direction of ON using speed reduction

    - junction logic
        - if either rear sensor is ON and turn is in turning tracker set then pivot in that direction
        - continue to turn until both front are ON again then continue to line follow

- reel detect loop
    - at first bay turn, activate reel detection loop until found
    - reel detection uses distance sensor on side to check each box before turning
        - if output is HIGH, initiate turn 
        - if output is LOW, increment rack[m][n] += 1 and continue

- pickup/unload loop
    - reel identification (resistance detection) lights corresponding LED
    - based on reel picked up, determine which unloading bay to return to (by resistance)
    - turn tracker code determines path


